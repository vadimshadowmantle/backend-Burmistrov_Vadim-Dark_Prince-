# Cafe Report

Программа читает заказы кофейни из файла `data/orders.csv` и печатает отчёт:
количество заказов, выручку, средний чек и суммы по официантам.

Внешних библиотек нет, нужен только Python 3.10 или новее.

## Запуск

Команды выполняются из корня проекта — из папки, в которой лежит код программы.

```
python -m venv .venv
python -m app.main
python -m unittest discover -s tests -v
```

На Windows вместо `python` может потребоваться `py`, а окружение активируется
командой `.\.venv\Scripts\Activate.ps1`.

## Ожидаемый результат

```
ОТЧЁТ ПО ЗАКАЗАМ
----------------------------------
Заказов: 6
Выручка: 3 702,00 ₽
Средний чек: 617,00 ₽
----------------------------------
Анна Петрова      770,00 ₽
Игорь Смирнов     1 793,00 ₽
Мария Ким         1 139,00 ₽
```

Тесты дают строку `Ran 9 tests` и `OK`.

## Структура проекта
```
cafe-report/
├── app/ — код программы
│ ├── main.py — точка запуска
│ ├── services/ — правила проекта: заказы и отчёт
│ └── utils/ — вспомогательные функции: суммы и строки
├── data/ — данные (orders.csv)
├── docs/ — описание проекта
├── tests/ — тесты
├── .gitignore — правила Git
├── requirements.txt — список зависимостей
└── README.md — инструкция по запуску

## Назначение папок

├── `app/` - код проги.
├── `app/services/` - правила проекта: заказы и отчёт.
├── `app/utils/` - вспомогательные функции: суммы и строки.
├── `data/` - данные (orders.csv).
├── `docs/` - описание проекта.
├── `tests/` - тесты.